# --- Core ---
Django==5.2.* # Dùng bản 5.2.x mới nhất (LTS)
psycopg2-binary            # Driver PostgreSQL
django-unfold              # Modern admin interface

# --- API (Cho Vue.js) ---
djangorestframework>=3.15  # Bản mới hỗ trợ Django 5
django-cors-headers

# --- Auth ---
djangorestframework-simplejwt

# --- Tiện ích ---
python-dotenv
Pillow
django-filter
python-slugify

# --- Editor ---
# Django CKEditor 5 - Modern WYSIWYG editor
django-ckeditor-5